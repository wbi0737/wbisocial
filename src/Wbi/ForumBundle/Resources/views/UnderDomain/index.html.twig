{% extends "WbiForumBundle::layout.html.twig" %}

{% block wbiforum_body %}
<h1>Underdomain list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Label</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('underdomain_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.label }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('underdomain_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('underdomain_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('underdomain_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock %}