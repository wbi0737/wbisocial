{% extends "WbiForumBundle::layout.html.twig" %}

{% block wbiforum_body %}

<ul class="breadcrumb">
        <li><a href="#">{{ entity.underdomain.domain.label }}</a> <span class="divider">/</span></li>
		<li><a href="#">{{ entity.underdomain.label }}</a> <span class="divider">/</span></li>
        <li><a href="#">{{ entity.postAutheur }}</a> <span class="divider">/</span></li>
        <li class="active">commentaires</li>
     </ul>

<caption>
    <h4>Liste des Commentaires</h4>
</caption>
<table class="table table-hover table-bordered table-striped table-condensed">
<thead>
    <tr>
	      <th>Commentaire</th>
		  <th>Autheur</th>
		  <th>Contenu</th>
		  <th>Date</th>
	</tr>
</thead>

<tbody>
{% for comments in entity.comments %}
	<tr>
                   
            <td>{{ comments.id }}</a></td>
			<td>{{ comments.user.username }}</td>
			<td>{{ comments.commentContent }}</td>
			<td>{{ comments.commentDate|date('Y-m-d H:i:s') }}</td>
			
			
   </tr>
 
         
    


        
	{% endfor %}
	
    </tbody>
</table>
</table>
<form class="well">
  <fieldset>
    <legend>Ajouter un commentaire</legend>
        <br/>
    <textarea  id="textarea" class="input-xxlarge" rows="3"></textarea>
    <p class="help-block">Vous pouvez agrandir la fenêtre</p>
    <button class="btn btn-primary" type="submit">Envoyer <i class="icon-white icon-ok-sign"></i> </button>
  </fieldset>
</form>
 {% endblock %}